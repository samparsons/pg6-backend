version: '3'
services:
  db: 
    image: mysql:8
    environment:
    - MYSQL_ROOT_PASSWORD=root
    - MYSQL_DATABASE=foodboxdb
    - MYSQL_USER=user
    - MYSQL_PASSWORD=userpasswd
    restart: always
    ports: 
    - 3306:3306
    volumes:
    - foodboxdb:/var/lib/mysql
    
  foodbox-service-rest:
    container_name: foodbox-service-rest
    restart: on-failure
    image: foodbox-service-rest
    build:
      context: ./
      dockerfile: Dockerfile
    depends_on:
    - foodboxdb
    ports:
    - 8081:8081
    environment:  
      - DATABASE_HOST=db  
      - DATABASE_USER=user
      - DATABASE_PASSWORD=userpasswd  
      - DATABASE_NAME=foodboxdb  
      - DATABASE_PORT=3306
    
volumes:
  foodboxdb: