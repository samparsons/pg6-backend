version: '3'
services:
  foodboxdb:
    container_name: foodboxdb
    image: mysql:latest
    environment:
    - MYSQL_ROOT_PASSWORD=
    - MYSQL_DATABASE=foodboxdb
    restart: always
    ports:
    - 3306:3306
    volumes:
    - foodbox-db:/var/lib/mysql
    network_mode: host
    
  foodbox-service-rest:
    container_name: foodbox-service-rest
    image: foodbox-service-rest
    build:
      context: ./
      dockerfile: Dockerfile
    depends_on:
    - foodbox-db
    ports:
    - 8081:8081
    restart: always
    network_mode: host
    
volumes:
  foodboxdb: